/*
 * {{group.name}}/{{group.alias}}/main.c
 * Automatically generated file
 */

#include "board.h"
#include <dpm/gnss/ublox.h>
#include <halm/generic/lifetime_timer_64.h>
#include <xcore/interface.h>
#include <assert.h>
#include <stdio.h>
#include <string.h>
/*----------------------------------------------------------------------------*/
#define TEXT_BUFFER_SIZE 128

struct Context
{
  struct Timer64 *chrono;
  struct Ublox *receiver;
  struct Interface *streamText;
  struct Interface *streamWork;
  struct Pin led;
  unsigned int pass;
  enum FixType fix;
};
/*----------------------------------------------------------------------------*/
static const uint32_t testUartRates[] = {9600, 57600, 115200, 230400, 460800};
static const uint32_t testWorkRate = 230400;
/*----------------------------------------------------------------------------*/
static void flushSerialTask(void *argument)
{
  struct Context * const context = argument;
  char buffer[TEXT_BUFFER_SIZE];

  while (ifRead(context->streamText, buffer, sizeof(buffer)) != 0);
}
/*----------------------------------------------------------------------------*/
static void onSerialEvent(void *argument)
{
  struct Context * const context = argument;
  uint32_t available;

  if (ifGetParam(context->streamText, IF_RX_AVAILABLE, &available) == E_OK)
  {
    if (available)
      wqAdd(WQ_DEFAULT, flushSerialTask, context);
  }
}
/*----------------------------------------------------------------------------*/
static void onConfigFinished(void *argument, bool status)
{
  struct Context * const context = argument;
  const uint64_t localtime = timerGetValue64(context->chrono);
  uint32_t rate = context->pass ?
      testUartRates[context->pass - 1] : testWorkRate;
  char buffer[TEXT_BUFFER_SIZE];

  sprintf(buffer, "%-10lu CFG %u %s\r\n",
      (unsigned long)localtime, (unsigned int)rate, status ? "true" : "false");
  ifWrite(context->streamText, buffer, strlen(buffer));

  if (!status)
  {
    rate = 0;

    while (!rate)
    {
      if (++context->pass > ARRAY_SIZE(testUartRates))
        context->pass = 0;

      if (context->pass > 0)
      {
        if (testUartRates[context->pass - 1] == testWorkRate)
          continue;
        rate = testUartRates[context->pass - 1];
      }
      else
        rate = testWorkRate;
    }

    pinWrite(context->led, BOARD_LED_INV);
    ifSetParam(context->streamWork, IF_RATE, &rate);
    ubloxReset(context->receiver, testWorkRate);
  }
}
/*----------------------------------------------------------------------------*/
static void onPositionReceived(void *argument,
    int32_t lat, int32_t lon, int32_t alt, int32_t msl)
{
  struct Context * const context = argument;
  const uint64_t localtime = timerGetValue64(context->chrono);
  const uint64_t timestamp = ubloxGetSolutionTime(context->receiver);
  char buffer[TEXT_BUFFER_SIZE];

  const unsigned long iPart = timestamp / 1000000;
  const unsigned long fPart = (timestamp % 1000000) / 1000;

  sprintf(buffer, "%-10lu POS %lu.%03lu %li %li %li %li\r\n",
      (unsigned long)localtime, iPart, fPart,
      (long)lat, (long)lon, (long)alt, (long)msl);
  ifWrite(context->streamText, buffer, strlen(buffer));
}
/*----------------------------------------------------------------------------*/
static void onPrecisionReceived(void *argument, uint32_t hAccuracy,
    uint32_t vAccuracy, uint32_t sAccuracy)
{
  struct Context * const context = argument;
  const uint64_t localtime = timerGetValue64(context->chrono);
  char buffer[TEXT_BUFFER_SIZE];

  const long hAcc = hAccuracy != UINT32_MAX ? (long)hAccuracy : -1;
  const long vAcc = vAccuracy != UINT32_MAX ? (long)vAccuracy : -1;
  const long sAcc = sAccuracy != UINT32_MAX ? (long)sAccuracy : -1;

  sprintf(buffer, "%-10lu ACC %li %li %li\r\n",
      (unsigned long)localtime, hAcc, vAcc, sAcc);
  ifWrite(context->streamText, buffer, strlen(buffer));
}
/*----------------------------------------------------------------------------*/
static void onSatelliteCountReceived(void *argument,
    const struct SatelliteInfo *info)
{
  struct Context * const context = argument;
  const uint64_t localtime = timerGetValue64(context->chrono);
  char buffer[TEXT_BUFFER_SIZE];

  sprintf(buffer, "%-10lu SAT %u %u %u %u %u\r\n",
      (unsigned long)localtime,
      (unsigned int)info->gps, (unsigned int)info->glonass,
      (unsigned int)info->beidou, (unsigned int)info->galileo,
      (unsigned int)info->sbas);
  ifWrite(context->streamText, buffer, strlen(buffer));
}
/*----------------------------------------------------------------------------*/
static void onStatusReceived(void *argument, enum FixType fix, uint16_t pdop)
{
  struct Context * const context = argument;
  const uint64_t localtime = timerGetValue64(context->chrono);
  char buffer[TEXT_BUFFER_SIZE];

  context->fix = fix;
  if (context->fix < FIX_3D)
    pinWrite(context->led, BOARD_LED_INV);

  sprintf(buffer, "%-10lu FIX %u %u\r\n",
      (unsigned long)localtime, (unsigned int)fix, (unsigned int)pdop);
  ifWrite(context->streamText, buffer, strlen(buffer));
}
/*----------------------------------------------------------------------------*/
static void onTimeReceived(void *argument, uint64_t timestamp, int8_t leaps)
{
  struct Context * const context = argument;
  const uint64_t localtime = timerGetValue64(context->chrono);
  char buffer[TEXT_BUFFER_SIZE];

  if (context->fix >= FIX_3D)
    pinToggle(context->led);

  const unsigned long iPart = timestamp / 1000000;
  const unsigned long fPart = timestamp % 1000000;

  sprintf(buffer, "%-10lu PPS %lu.%06lu %i\r\n",
      (unsigned long)localtime, iPart, fPart, (int)leaps);
  ifWrite(context->streamText, buffer, strlen(buffer));
}
/*----------------------------------------------------------------------------*/
static void onVelocityReceived(void *argument,
    int32_t north, int32_t east, int32_t down)
{
  struct Context * const context = argument;
  const uint64_t localtime = timerGetValue64(context->chrono);
  const uint64_t timestamp = ubloxGetSolutionTime(context->receiver);
  char buffer[TEXT_BUFFER_SIZE];

  const unsigned long iPart = timestamp / 1000000;
  const unsigned long fPart = (timestamp % 1000000) / 1000;

  sprintf(buffer, "%-10lu VEL %lu.%03lu %li %li %li\r\n",
      (unsigned long)localtime, iPart, fPart,
      (long)north, (long)east, (long)down);
  ifWrite(context->streamText, buffer, strlen(buffer));
}
/*----------------------------------------------------------------------------*/
int main(void)
{
  boardSetupClockPll();
  boardSetupDefaultWQ();

  const struct Pin led = pinInit(BOARD_LED);
  pinOutput(led, BOARD_LED_INV);

  struct Timer * const chronoTimer = boardSetupTimer();
  struct Timer * const stateTimer = boardSetupTimerAux();

  const struct LifetimeTimer64Config chronoConfig = {
      .timer = chronoTimer
  };
  struct Timer64 * const chrono = init(LifetimeTimer64, &chronoConfig);
  assert(chrono != NULL);
  timerEnable(chrono);

  struct Interrupt * const pps = boardSetupSensorEvent(INPUT_RISING,
      PIN_PULLDOWN);

  struct Interface * const streamText = boardSetupSerial();
  ifSetParam(streamText, IF_RATE, &(uint32_t){115200});

  struct Interface * const streamWork = boardSetupSerialAux();
  ifSetParam(streamWork, IF_RATE, &testWorkRate);

  const struct UbloxConfig config = {
      .chrono = chrono,
      .pps = pps,
      .serial = streamWork,
      .timer = stateTimer,
      .wq = WQ_DEFAULT,
      .rate = 5,
      .elevation = 10
  };
  struct Ublox * const receiver = init(Ublox, &config);
  assert(receiver != NULL);

  struct Context context = {
      .chrono = chrono,
      .receiver = receiver,
      .streamText = streamText,
      .streamWork = streamWork,
      .led = led,
      .pass = 1,
      .fix = FIX_NONE
  };

  ifSetCallback(streamText, onSerialEvent, &context);
  ubloxSetCallbackArgument(receiver, &context);
  ubloxSetConfigFinishedCallback(receiver, onConfigFinished);
  ubloxSetPositionReceivedCallback(receiver, onPositionReceived);
  ubloxSetPrecisionReceivedCallback(receiver, onPrecisionReceived);
  ubloxSetSatelliteCountReceivedCallback(receiver, onSatelliteCountReceived);
  ubloxSetStatusReceivedCallback(receiver, onStatusReceived);
  ubloxSetTimeReceivedCallback(receiver, onTimeReceived);
  ubloxSetVelocityReceivedCallback(receiver, onVelocityReceived);

  ubloxEnable(receiver);
  ubloxReset(receiver, testWorkRate);

  /* Initialize and start Work Queue */
  wqStart(WQ_DEFAULT);

  return 0;
}
